#include <stdio.h>
#include <stdlib.h>
void Pr(int *i);
int main() {
   /* struct check {
        int data;
        struct check *head;
    };

    struct check *hello;
    hello = (struct check *)malloc(sizeof(struct check));
    hello->data = 1;*/
    int a[2];
    a[0] = 3;
    printf("%d\n", a[1]);
    return 0;
}
